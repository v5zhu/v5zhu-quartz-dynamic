<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="chos-web"/>
    <dubbo:consumer timeout="${zookeeper.provider.timeout}"/>
    <!-- 使用multicast广播注册中心暴露发现服务地址 -->
    <dubbo:registry address="${zookeeper.registry.address}"/>

    <!--用户分组服务-->

    <dubbo:reference id="taskTest" interface="com.v5zhu.quartz.api.TaskTest"
    protocol="dubbo"/>
    <dubbo:reference id="taskService" interface="com.v5zhu.quartz.api.TaskService"
                     protocol="dubbo"/>
</beans>